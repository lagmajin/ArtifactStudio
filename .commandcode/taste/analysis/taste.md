# analysis
- When investigating what features are missing or incomplete in the Artifact project, do NOT trust doc/analysis files (`docs/analysis/`, `docs/planned/`) at face value — many are outdated. Always verify claims by searching the actual source code (.cppm/.ixx) for real implementations before reporting status. The user has corrected this multiple times: OCIO rated 🔴5% in gap analysis was actually 🟢75% implemented; MFR was written up as a full milestone under the assumption it was absent, but was actually already implemented with farm distribution + checkpoint recovery. The user proactively questions milestone premises when their own knowledge of the codebase contradicts the assistant's doc-based claims. The color system audit (2026-08-02) reinforced this: the user explicitly demanded 「実際のコードを見て」rather than accepting a rough summary. Confidence: 0.95
- When auditing/verifying whether a feature has production-level implementation (プロダクションレベルの実装), reading only headers/module interface files (.ixx/.h) is insufficient — must also read the actual implementation files (.cppm/.cpp) to determine whether real logic exists behind the interface. The user caught a broad audit sweep that rated components based on .ixx headers alone and demanded re-verification against actual .cppm implementations. Superficial header-level audits that don't check implementation files will be rejected. Confidence: 0.80
- When the user explicitly requests a detailed subsystem audit (「大まかにではなく細かく精査してほしい」「実際のコードを見て」), they expect a systematic code-level analysis: read every source file in the subsystem, evaluate per-component with quality scores (🟢/🟡/🟠/🔴), cite actual code snippets as evidence, identify specific issues (code duplication, missing pieces, quality gaps, architectural inconsistencies like dual implementations), and benchmark against industry standards where applicable (e.g. SMPTE ST 2084 constants, DaVinci Resolve feature parity). Save to `docs/analysis/<TOPIC>_AUDIT_YYYY-MM-DD.md`. This is distinct from feature-gap analysis (what's missing vs AE/Nuke), missing-modules analysis (what doesn't exist at all), and cross-app comparison — it's an inward-facing code quality and completeness audit of an existing subsystem. Confidence: 0.70
- After a high-quality subsystem audit is delivered and well-received, the user may initiate a full-codebase sequential audit sweep — "今のようなレポートを順番に全モジュールをしらべてみて" (investigate all modules in order like the report you just did). The user expects the same depth applied module-by-module in dependency order, not a single monolithic document. This is a distinct workflow track from feature-gap analysis and cross-app comparison — an inward-facing code-quality sweep of every existing subsystem. Recurring request: the user repeated it as 「各モジュールのソース調べてプロダクションレベルに到達してるか調べてほしい」, with whole-studio scope covering all sibling repos (Artifact, ArtifactCore, ArtifactWidgets, ArtifactRenderer, ArtifactPr) plus forked third-party libs (libs/DiligentEngine). Confidence: 0.70
- Even during broad codebase audit sweeps, every component evaluation must be verified against actual implementation files (.cppm), not just module interface files (.ixx) or outdated analysis docs. The user explicitly rejected a broad sweep that rated 16 categories based on header-level reading, demanding re-verification: 「本当にソース調べてるかどうか？もう一度プロダクションレベルの実装がされてるか精査してくれ」(are you really checking the source? re-audit for production-level implementation). Broad scope does not justify shallow depth — each component still needs .cppm-level verification. Confidence: 0.75
- When the user asks "is this really enough / sufficient?" (「本当にこれで十分なのか？」) after receiving a milestone or assessment, they are challenging the ambition level of the analysis — not seeking reassurance. The correct response is to re-examine critically against professional/industry-standard tools (e.g. Deadline, Tractor, Smedge, HQueue, AFANASY for render farm), identify gaps the initial analysis missed, and produce a structured gap analysis with priority levels — not to defend the original assessment. The user thinks at the system level and expects the assistant to proactively identify what was overlooked. Confidence: 0.75
- The user periodically probes whether a category/subsystem has enough breadth to be considered complete (「〇〇の数たりてるかな種類」/「やりつくしたかな」/「コンポジションのAPIとか見て　少ないか　質は大丈夫か　機能追加の余地あるか」) — e.g. glow effect variants, the Composition API surface (2026-08-21), and most recently the full UI widget set of a subproject (2026-08-22: 「今ってArtifactPrのウィジェットってどれくらいあるのかな　完成度は？」). The pattern applies beyond effect categories to subsystem API surfaces AND to UI widget inventories, and uses a consistent tripartite frame: coverage/count sufficiency, implementation quality, and room for feature additions. This is a breadth/saturation check, distinct from gap analysis: they want a source-verified enumeration/inventory of what exists (distinguishing real implementations from stubs — for widgets: implemented/partial/stub classification with per-item file+line-count evidence, totals, and identification of the biggest holes) plus a definitive verdict on whether to add more or consider the category done. When the category is already richly covered, the expected recommendation is to shift effort to quality (GPU/runtime acceptance) and combination presets over adding still more variants. Confidence: 0.7
- For ArtifactPr completeness questions, the user also benchmarks against the in-house flagship app itself (Artifact.exe), not only external industry references (AE/Nuke/Premiere): 「Artifact.exeに見劣りするものないかね？」(2026-08-22). Expected deliverable: a source-verified feature-surface inventory of the reference app (menus/panels/effects counts, layer types, subsystems — e.g. counted 141 registered effects, 57 widgets vs ArtifactPr's 25, 40 layer files) diffed against the subproject, structured as: scale-metric differences first, a per-domain missing-feature table, an honest section noting domains where the subproject actually leads (e.g. NLE ripple/roll/slip/slide trim absent from the main app's timeline), then a prioritized gap list whose priorities are justified against existing dev-policy documents (e.g. GPU preview deprioritized because the 2026-07-27 開発方針 doc defers it). Confidence: 0.6
- When a benchmark/gap analysis reveals duplication between two in-house surfaces (ArtifactPr re-implementing what Core/Artifact already has), the natural user follow-up is consolidation — they immediately asked 「じゃあコア部分は二重実装にならないようにArtifactに近づけたい」 (2026-08-22) after receiving the ArtifactPr-vs-Artifact comparison. Treat deduplication of core mechanisms as a standing priority once surfaced, and expect it to be scoped through explicit user decisions on breadth (minimal vs all-duplication tracks) and future-proofing (plain QUndoCommand vs SerializableCommand for collaboration groundwork). Confidence: 0.65
