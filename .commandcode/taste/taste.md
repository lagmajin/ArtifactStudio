# communication-integrity
- Always read actual source code before answering — never rely solely on documents, speculation, or unchecked assumptions. If something is unverified or unclear, explicitly state 「未確認」rather than guessing or presenting speculation as fact. This applies to ALL responses, not just feature-gap investigations. The user issued this directive after being told that MFR was "not implemented" (which led to a full milestone being written), when in fact MFR was already implemented with farm distribution + checkpoint recovery — the assistant had trusted its own assumption rather than reading the source first. Applies equally to explore-subagent output: load-bearing claims returned by dispatched agents must be re-verified directly in source (grep/read citing file:line) before being presented as findings — applied 2026-08-21 in the offline-rendering audit (confirmed `const bool useMfr = false;` at ArtifactRenderQueueService.cppm:6660 and the QPainter fallback before reporting them). Reconfirmed 2026-08-22 during the core-dedup migration: the assistant paused mid-implementation to verify its own assumptions against actual source before writing dependent code — whether the JSON helpers lived in an anonymous namespace vs external linkage, which CMake targets actually contain Core's Command/Audio/Video modules (split STATIC libs, not the aggregate), and exact AudioMixer/AudioBus signatures. Extended 2026-08-22: this duty covers the assistant's own accumulated records too — when fresh exploration contradicts a previously recorded claim (the foundation-class audit found prior 「RandomStream 統合済み」 note contradicted by grep showing 0 external usages + mt19937 remnants), surface the contradiction openly and mark the item 要再確認 instead of repeating the stale record as fact. Confidence: 0.95

# analysis
See [analysis/taste.md](analysis/taste.md)
# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/


# cost-benefit-prioritization
- When deciding which types in the codebase to extend with operators/utilities, prioritize based on cross-codebase usage footprint, not just whether the type is a color/vector/etc. Types used only as local variables in a single translation unit (e.g. HSVColor/HSLColor used only inside ColorConversion) are low value for operator expansion. Types heavily used across multiple subsystems (e.g. DynVec2/3 used throughout AnimationDynamics) get higher priority. The user accepted this cost-benefit analysis — "value of wide cross-cutting impact" — without objection. Confidence: 0.70

# cppm-modules
- In C++20 module files (.cppm/.ixx), do NOT put `#include` directives in the module purview (after `module X;`). All includes must go in the Global Module Fragment (GMF) between `module;` and `export module X;`. The only exception is TBB includes which should be placed in GMF with all other includes, NOT in the purview. Confidence: 0.80

# project-workflow
See [project-workflow/taste.md](project-workflow/taste.md)

# project-code-style
See [project-code-style/taste.md](project-code-style/taste.md)
# project-submodule-boundary
- Do not modify, commit, or push the `ArtifactWidgets`, `libs/...`, or `third_party/*` submodules unless the user explicitly asks; treat them as read-only/external. New UI widgets are first placed under `Artifact/` and only promoted to `ArtifactWidgets/` later. Confidence: 0.85
- Treat `Artifact_dev_review/` as a read-only sandbox copy; do not edit files inside it. Only modify the `Artifact/` directory for code changes. Confidence: 0.70

# ux-design-philosophy
- Prefers innovative UX interactions with "wow factor" over standard drag-and-drop / conventional patterns. When asked for feature proposals (especially D&D/UI interactions), the user expects the assistant to brainstorm creative, surprising ideas that make users say 「そんなことできるの？」(you can do that?!), rather than straightforward conventional implementations. The user explicitly rejected a standard "drag effect from palette to layer" milestone as too plain/obvious (「地味だ」) and asked for more revolutionary interaction designs. Confidence: 0.80
- Prefers feature designs that are mode-rich (モードがいっぱいある) with many variations over a single-purpose tool — when brainstorming enhancements for the plane/solid layer, the user found the idea of "many modes" appealing (「モードがいっぱいあると面白そう」). Confidence: 0.55

# project-architecture
See [project-architecture/taste.md](project-architecture/taste.md)

# task-preference
- When presented with a choice between stability/bug-fix work and new feature development, prefers new feature creation (新機能作成). Reconfirmed 2026-08-21: offered four ArtifactPr tracks — build restore (assistant-recommended), export implementation, composite preview (new feature), engine testing — the user picked the new-feature option (合成プレビュー) over the recommended build-fix prerequisite. Also defers test infrastructure and small cleanups: in the 2026-08-22 multi-select scope question the user selected データ整合 + プレビュー強化 but left GTest 追加 and 小口改善 (bin 管理・autoSave 整理) unselected. Same pattern again 2026-08-22 (variable expansion): chose only the core mechanism (展開エンジン＋Property フック, 評価時展開) and deferred env ファイル / 変数一覧 UI / ArtifactPr 対応 to a later phase — for big infrastructure features, core engine first, UI/peripheral integration later. Confidence: 0.75

# language
- Communicates in Japanese and expects responses in Japanese. Confidence: 0.85

# project-direction
- Currently focuses feature development on image/plane/text/shape layers (画像・平面・テキスト・シェイプレイヤー) for the near term; feature brainstorming, gap analysis, and prioritization should center on these four layer types. The user reconfirmed this direction (2026-08-21), adding テキスト (text) layers to the previously observed plane/shape/image trio. Confidence: 0.75
- The in-house scripting language **ArtifactScript** (`ArtifactCore/src/Script/ArtifactScript/`) is a strategic investment: the user wants to evolve it (「自作言語...これでいきたいなぁ」) rather than rely on embedded third-party engines (AngelScript / CSharpScriptEngine). The user set six acceptance conditions for future language work (2026-08-21): ① syntax close to C++/C#, ② class support, ③ easy export, ④ simple edit→compile→run cycle, ⑤ Unity-style serialization, ⑥ fast edit→run (hot reload). Treat these as the goalposts when planning or implementing ArtifactScript work. Requirement ③ was explicitly clarified by the user (2026-08-21): 「簡単にエクスポート」 means a **C++ host binding API** (user answered 「Bですね」 when offered A–D interpretations) — a host registry (`ArtifactScriptHost`) where C++ side registers functions/types callable from scripts via normal call syntax — NOT script-file/package export. The agreed evolution order (captured in `MILESTONE_ARTIFACTSCRIPT_LANGUAGE_EVOLUTION_2026-08-21.md`): Phase 1 C++/C#-like syntax fixes (string concat, compound assignment, break/continue, invokeHook realization) → Phase 2 binding API → Phase 3 real class support (`new`/`this`/inheritance/multi-class) → Phase 4 Unity-style serialization (attributes, Inspector exposure, project save) → Phase 5 editor UI + hot-reload wiring. Confidence: 0.85
- ArtifactStudio intentionally maintains TWO independent in-house scripting systems with zero shared types: the **Expression Engine** (`ArtifactCore/src|include/Script/Expression/`, ~2,700 lines, AE-style property expressions — recursive-descent parser/evaluator, ternary/arrays/vector math, 43 builtins incl. wiggle/noise/loopIn-loopOut/linear-ease, already injected into Property keyframe evaluation with value/time/frameRate/keyframes, plus the ArtifactExpressionCopilotWidget UI) and **ArtifactScript** (behavior side). Treat this 棲み分け (Expression = per-property declarative evaluation; ArtifactScript = Unity-style component lifecycle/classes) as healthy — don't propose merging the languages. Known Expression gaps (updated 2026-08-21 after the low-impact fix pass): posterizeTime added and seedRandom exposed as an alias of randomSeeded; `index` variable now injectable via an optional `layerIndex` parameter on `AbstractProperty::evaluateValue` + Copilot preview (wiring the real layer index through the render path still pending); remaining gaps: thisComp/thisLayer injection on the normal render-time path, JS/Python/Ruby/VB style-switch API not wired to UI, toComp/fromWorld coordinate transforms, Pick Whip ↔ PropertyEditor linkage, empty stub modules in `Script/Engine/` plus an orphaned ScriptRuntime facade — the user explicitly chose to skip these as high-impact (影響大). When designing the ArtifactScript binding API (Phase 2), consider sharing `ScriptContext` + `BuiltinScriptVM` (working timeout-capable execution host) as a common runtime for both languages instead of duplicating host infrastructure. Confidence: 0.55

# dcc-domain-context
- The user's tooling frame of reference is Visual Studio / Windows C++ development — they self-describe as knowing only VS among comparable apps (「自分はVisualStudioしかしらない」, 2026-08-22). When discussing DCC-application conventions (Houdini/Maya/Nuke/Blender/pipeline tooling), explain via analogies to VS/dev-tooling equivalents (e.g. VS のプロパティ→環境変数 dialog) rather than assuming DCC familiarity. Confidence: 0.8
- The user treats DCC apps as a source of best-in-class design ideas and poses informal 「最強の〇〇はどのアプリ？」surveys — first instance 2026-08-22: environment-variable systems (usability/editability focus). Expected answer style: mechanism-level per-app comparison (houdini.env + packages/*.json + `$VAR` expansion working in every parameter field; maya.env simplicity; Nuke init.py/menu.py; studio-level Rez) with clear trade-off verdicts (editability vs expressiveness), not vague name-drops. Confidence: 0.55
