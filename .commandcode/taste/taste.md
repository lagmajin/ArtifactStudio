# communication-integrity
- Always read actual source code before answering — never rely solely on documents, speculation, or unchecked assumptions. If something is unverified or unclear, explicitly state 「未確認」rather than guessing or presenting speculation as fact. This applies to ALL responses, not just feature-gap investigations. The user issued this directive after being told that MFR was "not implemented" (which led to a full milestone being written), when in fact MFR was already implemented with farm distribution + checkpoint recovery — the assistant had trusted its own assumption rather than reading the source first. Applies equally to explore-subagent output: load-bearing claims returned by dispatched agents must be re-verified directly in source (grep/read citing file:line) before being presented as findings — applied 2026-08-21 in the offline-rendering audit (confirmed `const bool useMfr = false;` at ArtifactRenderQueueService.cppm:6660 and the QPainter fallback before reporting them). Reconfirmed 2026-08-22 during the core-dedup migration: the assistant paused mid-implementation to verify its own assumptions against actual source before writing dependent code — whether the JSON helpers lived in an anonymous namespace vs external linkage, which CMake targets actually contain Core's Command/Audio/Video modules (split STATIC libs, not the aggregate), and exact AudioMixer/AudioBus signatures. Extended 2026-08-22: this duty covers the assistant's own accumulated records too — when fresh exploration contradicts a previously recorded claim (the foundation-class audit found prior 「RandomStream 統合済み」 note contradicted by grep showing 0 external usages + mt19937 remnants), surface the contradiction openly and mark the item 要再確認 instead of repeating the stale record as fact. Confidence: 0.95
- When the user refers to a feature by a UI/Japanese display name (e.g. 「ミキサーレイヤー」) OR by an informal abbreviation/acronym (e.g. 「VP操作」), do NOT assume what the term maps to or that a matching class exists in source — grep the term itself plus likely expansions/English equivalents (Mixer/MixerLayer; VP → Viewport) across code and docs before planning. If the expansion resolves unambiguously in code, state the interpretation explicitly and proceed with that reading; if the name maps to no class, surface the mismatch and ask the user to disambiguate which code entity they mean (structured options) instead of guessing. Applied 2026-08-22 when 「ミキサーレイヤー」 turned out to have no corresponding image-layer class (only AudioMixer / an unimplemented ColorNodeType::LayerMixer stub / LayerBlendPipeline), and the assistant asked a disambiguating question before judging the 昇格 feasibility. Applied again 2026-09-02: 「VP操作」 was resolved by grepping `\bVP\b` across .ixx/.cppm/.cpp/.h (found = Diligent `Viewport`) plus TextViewport variants, with the interpretation stated to the user before investigating text-layer viewport interaction. A third outcome also exists — the name resolves to a *shared algorithm library*, not to any effect: 「カラーコレクションエフェクト」(2026-09-26) turned out to be no effect at all, only the CMake static library `ColorCollection` (`ColorCollection.ColorGrading` module) plus one differently-named consumer effect `ColorWheelsEffect`. Correct handling is to open the report by correcting the premise (「『Color Collection』という名前のエフェクトは存在しません」), then answer under the interpreted reading anyway — implemented-assets inventory followed by a file:line table of real defects in the library and its consumer — rather than stalling on a disambiguation question. Confidence: 0.6
- The same duty applies when a request's *wording* admits several UI interpretations, not just when a name is unclear: disambiguate before designing by presenting the candidate interpretations as structured `ask_user_question` options, each with a one-line description and an ASCII-art preview of the resulting layout, rather than guessing which variant was meant. Applied 2026-09-22: 「VisualStudioみたいなタブエリアが下のバージョンもほしい」 was resolved with 4 diagrammed options (per-pane bottom tab row / bottom-dock tab refresh / dual tab strips / window-bottom strip) before the milestone was updated. Applied again 2026-09-22 in the same dock-tab session: 「タイムラインにはロックとか未保存も追加したい」 was ambiguous in two independent dimensions — which tab surface carries the marks (Timeline dock tab / the composition editing-context tab row / a new timeline-internal tab row) and what 「ロック」 means (dock placement lock / panel edit lock / aggregation of the existing layer locks) — so two diagrammed questions were asked together, and the answer narrowed the request to the Timeline dock tab with 未保存のみ（ロックは対象外）. Confidence: 0.6

# analysis
See [analysis/taste.md](analysis/taste.md)

# feature-request-framing
- When the user names an EXTERNAL product as shorthand for a capability bundle (「ResharperやVisualAssistみたいな機能ほしい」, and by extension Figma/Blender/Nuke-style references), the name is not one feature — it is a bundle. Do not implement your own guess of what that product means. Instead: (1) first ground the request in what the local engine can actually support (read the parser/evaluator data model — does the AST carry source offsets, is arity/type recorded anywhere, does a structured diagnostics type exist) and report the gaps that determine cost; (2) then decompose the product into 3–4 concrete capability groups and present them as a structured `ask_user_question` multi-select, each with a one-line description plus an ASCII preview of the resulting UI and a note of what must be built from scratch. Do not ask which single feature of ReSharper they mean — the user is delegating that decomposition. Applied 2026-09-26: decomposed into ① 署名メタデータ基盤＋型付き補完＋シグネチャヘルプ ② 診断強化（問題一覧/位置付きエラー） ③ ナビゲーション/Pick Whip ④ フォーマッタ/クリーンアップ, paired with a 対象 question (live Expression editor vs a not-yet-existing script editor). Confidence: 0.6
# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/


# cost-benefit-prioritization
- When deciding which types in the codebase to extend with operators/utilities, prioritize based on cross-codebase usage footprint, not just whether the type is a color/vector/etc. Types used only as local variables in a single translation unit (e.g. HSVColor/HSLColor used only inside ColorConversion) are low value for operator expansion. Types heavily used across multiple subsystems (e.g. DynVec2/3 used throughout AnimationDynamics) get higher priority. The user accepted this cost-benefit analysis — "value of wide cross-cutting impact" — without objection. Confidence: 0.70

# cppm-modules
- In C++20 module files (.cppm/.ixx), do NOT put `#include` directives in the module purview (after `module X;`). All includes must go in the Global Module Fragment (GMF) between `module;` and `export module X;`. The only exception is TBB includes which should be placed in GMF with all other includes, NOT in the purview. Confidence: 0.80

# project-workflow
See [project-workflow/taste.md](project-workflow/taste.md)

# project-code-style
See [project-code-style/taste.md](project-code-style/taste.md)
# project-submodule-boundary
- Do not modify, commit, or push the `ArtifactWidgets`, `libs/...`, or `third_party/*` submodules unless the user explicitly asks; treat them as read-only/external. New UI widgets are first placed under `Artifact/` and only promoted to `ArtifactWidgets/` later. When a feature looks like it needs submodule work, verify the *consumer-side* API before assuming so — if the public interface takes a whole-frame object (e.g. `updateFrame(const QImage&)`) and the caller already holds the full frame locally, a rect-limited feature (ROI) is achievable with a single `frame.copy(x,y,w,h)` at the call site and the submodule boundary is never crossed (2026-09-26, scope ROI). Consequently prefer a CPU-side parent-repo path as stage 1 and defer any GPU/submodule version to a later stage gated on measurement, instead of blocking on a fork/patch decision up front. Confidence: 0.85
- Treat `Artifact_dev_review/` as a read-only sandbox copy; do not edit files inside it. Only modify the `Artifact/` directory for code changes. Confidence: 0.70

# ux-design-philosophy
See [ux-design-philosophy/taste.md](ux-design-philosophy/taste.md)
# project-architecture
See [project-architecture/taste.md](project-architecture/taste.md)

# task-preference
- When presented with a choice between stability/bug-fix work and new feature development, prefers new feature creation (新機能作成). Reconfirmed 2026-08-21: offered four ArtifactPr tracks — build restore (assistant-recommended), export implementation, composite preview (new feature), engine testing — the user picked the new-feature option (合成プレビュー) over the recommended build-fix prerequisite. Also defers test infrastructure and small cleanups: in the 2026-08-22 multi-select scope question the user selected データ整合 + プレビュー強化 but left GTest 追加 and 小口改善 (bin 管理・autoSave 整理) unselected. Same pattern again 2026-08-22 (variable expansion): chose only the core mechanism (展開エンジン＋Property フック, 評価時展開) and deferred env ファイル / 変数一覧 UI / ArtifactPr 対応 to a later phase — for big infrastructure features, core engine first, UI/peripheral integration later. Reconfirmed 2026-09-26: from a shortlist of implementable candidates the user took the recommended smallest win (wiring an already-implemented predicate) *and* a mid-sized feature (display-only viewer exposure post-process) in the same pass, skipping both the trivial one-liner (`static const bool` → runtime flag) and the large foundational work. So the selector is not strictly "smallest first" — it is "recommended first, plus at most one substantial item". And when the shortlist contains a zero-code option that stops a stale `Not Started` item from being re-implemented, the user takes that one *alone* and skips even the recommended code item — avoiding duplicate implementation of already-working code outranks picking a new feature (2026-09-26: chose only 「P1-12 の重複を文書で解消」 over the recommended ShowDiagonals one-liner and the P1-10 Clipping feature). Further reconfirmed 2026-09-26: after a report that ended with a 未対応 list plus a 確認事項 flagging an unverified assumption, the shortlist mixed one verification item with three real new features (10-effect preset wiring, LUTEffect GPU impl, a dedicated Color Wheels panel with an ASCII mock) — and the user took the verification item, marked 「(Recommended)」. So a clean-upe-your-own-work option presented as a peer choice does get picked over new features, and the recommendation marker appears to carry real weight in the `ask_user_question` form. Confidence: 0.85
- For a broad NEW feature area the user scopes the start down to a single self-contained, low-risk sub-feature instead of kicking off several at once: offered four accessibility axes (viewport magnifier / screen-reader depth / keyboard-only core editing / reduce-motion) with multi-select explicitly allowed and a 推奨 marker, the user picked only the recommended, self-contained viewport magnifier (2026-09-21) — chosen partly because the settings and UI for it already existed and only the drawing consumer was missing. This is distinct from audit-generated gap/fix shortlists, where the standing default is 「全部で」/「じゃあ修正を」 (handle all of them). Confidence: 0.5

# language
- Communicates in Japanese and expects responses in Japanese. Confidence: 0.85

# project-direction
- Currently focuses feature development on image/plane/text/shape layers (画像・平面・テキスト・シェイプレイヤー) for the near term; feature brainstorming, gap analysis, and prioritization should center on these four layer types. The user reconfirmed this direction (2026-08-21), adding テキスト (text) layers to the previously observed plane/shape/image trio. Confidence: 0.75
- The in-house scripting language **ArtifactScript** (`ArtifactCore/src/Script/ArtifactScript/`) is a strategic investment: the user wants to evolve it (「自作言語...これでいきたいなぁ」) rather than rely on embedded third-party engines (AngelScript / CSharpScriptEngine). The user set six acceptance conditions for future language work (2026-08-21): ① syntax close to C++/C#, ② class support, ③ easy export, ④ simple edit→compile→run cycle, ⑤ Unity-style serialization, ⑥ fast edit→run (hot reload). Treat these as the goalposts when planning or implementing ArtifactScript work. Requirement ③ was explicitly clarified by the user (2026-08-21): 「簡単にエクスポート」 means a **C++ host binding API** (user answered 「Bですね」 when offered A–D interpretations) — a host registry (`ArtifactScriptHost`) where C++ side registers functions/types callable from scripts via normal call syntax — NOT script-file/package export. The agreed evolution order (captured in `MILESTONE_ARTIFACTSCRIPT_LANGUAGE_EVOLUTION_2026-08-21.md`): Phase 1 C++/C#-like syntax fixes (string concat, compound assignment, break/continue, invokeHook realization) → Phase 2 binding API → Phase 3 real class support (`new`/`this`/inheritance/multi-class) → Phase 4 Unity-style serialization (attributes, Inspector exposure, project save) → Phase 5 editor UI + hot-reload wiring. Confidence: 0.85
- ArtifactStudio intentionally maintains TWO independent in-house scripting systems with zero shared types: the **Expression Engine** (`ArtifactCore/src|include/Script/Expression/`, ~2,700 lines, AE-style property expressions — recursive-descent parser/evaluator, ternary/arrays/vector math, 43 builtins incl. wiggle/noise/loopIn-loopOut/linear-ease, already injected into Property keyframe evaluation with value/time/frameRate/keyframes, plus the ArtifactExpressionCopilotWidget UI) and **ArtifactScript** (behavior side). Treat this 棲み分け (Expression = per-property declarative evaluation; ArtifactScript = Unity-style component lifecycle/classes) as healthy — don't propose merging the languages. Known Expression gaps (updated 2026-08-21 after the low-impact fix pass): posterizeTime added and seedRandom exposed as an alias of randomSeeded; `index` variable now injectable via an optional `layerIndex` parameter on `AbstractProperty::evaluateValue` + Copilot preview (wiring the real layer index through the render path still pending); remaining gaps: thisComp/thisLayer injection on the normal render-time path, JS/Python/Ruby/VB style-switch API not wired to UI, toComp/fromWorld coordinate transforms, Pick Whip ↔ PropertyEditor linkage, empty stub modules in `Script/Engine/` plus an orphaned ScriptRuntime facade — the user explicitly chose to skip these as high-impact (影響大). When designing the ArtifactScript binding API (Phase 2), consider sharing `ScriptContext` + `BuiltinScriptVM` (working timeout-capable execution host) as a common runtime for both languages instead of duplicating host infrastructure. Confidence: 0.55

# dcc-domain-context
- The user's tooling frame of reference is Visual Studio / Windows C++ development — they self-describe as knowing only VS among comparable apps (「自分はVisualStudioしかしらない」, 2026-08-22). When discussing DCC-application conventions (Houdini/Maya/Nuke/Blender/pipeline tooling), explain via analogies to VS/dev-tooling equivalents (e.g. VS のプロパティ→環境変数 dialog) rather than assuming DCC familiarity. Confidence: 0.8
- The user treats DCC apps as a source of best-in-class design ideas and poses informal 「最強の〇〇はどのアプリ？」surveys — first instance 2026-08-22: environment-variable systems (usability/editability focus). Expected answer style: mechanism-level per-app comparison (houdini.env + packages/*.json + `$VAR` expansion working in every parameter field; maya.env simplicity; Nuke init.py/menu.py; studio-level Rez) with clear trade-off verdicts (editability vs expressiveness), not vague name-drops. Confidence: 0.55
- The user's app is explicitly AE-style (AE風アプリ), and they treat After Effects as the authoritative reference for what counts as *correct* feature behavior — a usage distinct from (and beyond) AE-as-gap-analysis-baseline. When the user asks how a feature should behave (e.g. 「AE風アプリってどうグリッド表示するのが正しいの？」), ground the answer in AE's actual runtime behavior: AE's grid (View > Show Grid / Ctrl+') is an infinite grid anchored to the composition origin (0,0) and spanning the full viewport into the pasteboard — not clipped to the composition fill region — with zoom-dependent spacing LOD. Generalizable principle: keep *infinite* overlays (grid, guides = viewport-wide, origin-anchored) separate from *composition-ratio* overlays (safe margins, proportional grid, comp boundary = clipped to the comp frame). Reconfirmed 2026-09-22: after a path-consistency verification report, the user's follow-up was 「AEと同じようにしたいが修正は」 — AE parity is the stated *fix goal*, and the expected response is to pin AE's real behavior first (web-search of AE's documented semantics — stopwatch = animation-enable toggle that inserts a key on enable and deletes all keys on disable, diamond = add/remove key at current time, Auto-Keyframe applies the keying-set to any value change) and then present the current-vs-AE divergences as a numbered 修正候補 list with an ask_user_question scope choice. Confidence: 0.6
