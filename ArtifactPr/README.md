# ArtifactPr

Pr-like editor prototype that reuses `ArtifactCore` as the shared foundation.

This project is intentionally separate from `Artifact` so the two apps can
grow in different directions without forcing the same UI or timeline shape.
