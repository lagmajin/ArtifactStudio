#pragma once

// Kept as a compatibility include for sources in this directory.  The
// declaration itself is public so every translation unit uses the same type.
#include "../include/ExternalRenderJobSchema.h"
