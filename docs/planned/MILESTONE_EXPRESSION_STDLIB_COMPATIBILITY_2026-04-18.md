# MILESTONE: After Effects エクスプレッション標準ライブラリ互換性

作成日: 2026-04-18
優先度: 🔴 最高
推定工数: 3日

---

## 概要

既存の式評価エンジンに対して、After Effects 標準のエクスプレッション関数と100%互換性のある標準ライブラリを実装する。

エンジン本体は既に完成しており、不足しているのは互換レイヤーだけです。

---

## ✅ 現在の状況

✅ 式パーサー: 100% 完成
✅ 評価エンジン: 100% 完成
✅ 演算子優先順位: AEと完全一致
✅ 値型システム: Number/Array/Vector/Boolean 全て対応済み
❌ AE標準グローバル変数: 未実装
❌ AE標準ライブラリ関数: 10% 実装済み

---

## 🎯 実装目標

After Effects 2025 のエクスプレッション言語との **100% ソース互換性**

既存のAEエクスプレッションをコピペしてそのまま動作すること。

---

## 📋 実装リスト

### Phase 1 (1日目)

| 関数名 | 状況 | 優先度 |
|--------|------|--------|
| `time` | ✅ 既存 | 💯 |
| `value` | ✅ 既存 | 💯 |
| `index` | ⚠️ 未実装 | 💯 |
| `thisLayer` | ⚠️ 未実装 | 💯 |
| `thisComp` | ⚠️ 未実装 | 💯 |
| `linear()` | ⚠️ 未実装 | 💯 |
| `ease()` | ⚠️ 未実装 | 💯 |
| `easeIn()` | ⚠️ 未実装 | 💯 |
| `easeOut()` | ⚠️ 未実装 | 💯 |
| `clamp()` | ✅ 既存 | 💯 |

### Phase 2 (2日目)

| 関数名 | 優先度 |
|--------|--------|
| `sin()` / `cos()` / `tan()` | 🔴 |
| `degToRad()` / `radToDeg()` | 🔴 |
| `length()` | 🔴 |
| `distance()` | 🔴 |
| `normalize()` | 🔴 |
| `lookAt()` | 🟠 |
| `random()` | 🟠 |
| `seedRandom()` | 🟠 |

### Phase 3 (3日目)

| 関数名 | 優先度 |
|--------|--------|
| `sourceRectAtTime()` | 🔴 |
| `loopOut()` | 🟠 |
| `loopIn()` | 🟠 |
| `posterizeTime()` | 🟠 |
| `wiggle()` | 🟠 |
| `smooth()` | 🟡 |

---

## 🛠 実装計画

1.  **`ExpressionEvaluator.cppm`** に互換レイヤークラスを追加
2.  グローバルスコープにAE互換関数を登録
3.  レイヤー参照システムを統合
4.  単体テストをAEの出力結果と比較
5.  既存の式を破壊しないことを確認

---

## ✅ 完了条件

- AEの公開エクスプレッションサンプル100個が全て同じ結果を返す
- `linear()` `ease()` の動作がpixel単位で一致
- 既存のプロジェクトファイルが全て破壊されない

---

## 💡 技術的メモ

既存のエンジンは非常に良く設計されており、互換レイヤーの追加は全く難しくありません。
このマイルストーンが完了すると、インターネット上に存在する殆ど全てのAfter Effectsエクスプレッションがそのまま動作するようになります。
