# M-SC-2 Phase 2 Execution
**作成日:** 2026-04-21

## Execution Notes

- 最初に刺すのは `ArtifactCompositionRenderWidget`
- 次に `ArtifactTimelineWidget`
- その後 `ArtifactLayerPanelWidget` と `ArtifactAssetBrowser`
- `ArtifactInspectorWidget` は property / selection の編集導線が固まってからでよい
- `ArtifactPlaybackShortcuts` は global / workspace 共通の playback context として扱う
