# タイムライン: キーフレーム編集 マイルストーン

最終更新: 2026-04-11T07:22:10.963Z

目的:
- タイムライン上でキーフレームを作成・編集・削除・移動できるようにする
- 補間（イージング）と再生プレビューをサポート
- キーフレームをプロジェクトとともにシリアライズして保存する

範囲:
- モデル: Keyframe 構造、バージョン、変更通知API
- シリアライズ: 保存/読み込み、後方互換性
- UI: トラック表示、キーフレームハンドル、編集操作（追加/移動/削除/複製）
- 補間: リニア、イーズ、カスタム（ベジェ等）
- テスト: 単体テストと視覚テスト（ネスト・変換・不透明度・境界ケース）

実装ステップ:
1. Keyframe モデル定義と通知API（現在着手）
2. シリアライズ実装（プロジェクト保存/読み込み）
3. UI: トラックビュー、ハンドル操作、ショートカット
4. 補間エンジンと再生統合（プレビュー）
5. テストとドキュメント整備

依存:
- WIDGET_MAP に定義されたタイムライン/トラックの既存コンポーネント
- レイヤーの時間変換ユーティリティ

備考:
- まずモデル実装（timeline-keyframe-model）を完了させ、APIを安定させてからUI実装に進む。
- 実装状況は docs/ に逐次反映する。

## 実装メモ

- Artifact/include/Widgets/Timeline/ArtifactTimelineKeyframeModel.ixx を追加し、UI 向けの Keyframe 集約モデルを定義しました。
- Artifact/src/Widgets/Timeline/ArtifactTimelineKeyframeModel.cppm に簡易実装を追加しました（プロジェクトサービス経由でレイヤー/プロパティを取得し、AbstractProperty の add/remove/getKeyFrames を呼ぶ）。
- まだ統合は行っていません。次は UI 側でこのモデルを利用して marker の集約と編集アクション経路を実装します。
